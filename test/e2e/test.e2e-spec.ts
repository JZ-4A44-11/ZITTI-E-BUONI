import * as request from 'supertest'

describe('void test', () => {
  it('should be defined', () => {
    /** Actually i don't have any test to run */
    return expect(request).toBeDefined()
  })
})
